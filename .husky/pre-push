# Thorough pre-push gate. Mirrors the CI build-and-test pipeline so a
# clean push stays green on Actions. Escape hatch: `git push --no-verify`.
set -e
npm run lint
npx tsc --noEmit
npm test
npm run check:guardrails
npm run build
