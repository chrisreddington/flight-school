node_modules/
.next/
.next-worker/
dist/
dist-worker/
out/
build/
coverage/
playwright-report/
test-results/
.modules/

# Generated by agentic workflows.
.github/workflows/*.lock.yml
.github/workflows/*.md

# Workflow YAML follows GitHub Actions conventions (and several are
# generated by gh-aw). Keep prettier out — actionlint is authoritative.
.github/workflows/*.yml
.github/workflows/*.yaml

# Markdown is handled by markdownlint-cli2 (which has its own ignore set
# and stricter rules for prose). Keep prettier off `.md` to avoid two
# tools rewriting the same files.
*.md
*.mdx

# Lockfiles + generated assets.
package-lock.json
*.svg
*.ico
*.png

# Snapshot fixtures (vitest)
**/__snapshots__/**
